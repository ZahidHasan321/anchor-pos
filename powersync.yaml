port: 8080

replication:
  connections:
    - type: postgresql
      uri: !env PS_REPLICATION_URL
      sslmode: disable

storage:
  type: postgresql
  uri: !env PS_STORAGE_URL

client_auth:
  jwks:
    uri: !env PS_JWKS_URL

sync_rules:
  path: /app/powersync-rules.yaml
