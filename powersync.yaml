port: 8080

replication:
  uri: !env POWERSYNC_DATABASE_URL

storage:
  type: postgresql
  uri: !env POWERSYNC_STORAGE_URL

sync_rules:
  path: /app/powersync-rules.yaml

auth:
  jwks_url: !env POWERSYNC_JWKS_URL
