port: 8080

replication:
  connections:
    - type: postgresql
      uri: !env PS_REPLICATION_URL

storage:
  type: postgresql
  uri: !env PS_STORAGE_URL

client_auth:
  jwks_uri: !env PS_JWKS_URL
  audience: ["powersync-dev", "powersync"]

sync_rules:
  path: /app/powersync-rules.yaml
